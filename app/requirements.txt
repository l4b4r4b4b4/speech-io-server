# requirements.txt

# Core Web Framework
fastapi
uvicorn[standard] # Standard includes WebSockets, HTTP/2 and other extras
gunicorn # Production WSGI HTTP server to manage Uvicorn workers

# Machine Learning & Audio
torch
torchaudio
numpy
soundfile
huggingface_hub
descript-audio-codec
safetensors
openai-whisper

# Configuration & Utilities
pydantic
python-dotenv # Used ONLY for initial config seeding if config.yaml missing
Jinja2
python-multipart # For file uploads
requests # For health checks or other potential uses
PyYAML # For parsing presets.yaml AND primary config.yaml
tqdm

# Audio Post-processing
pydub
praat-parselmouth # For unvoiced segment removal
PyYAML # For parsing presets.yaml
hf-transfer
# Production & Deployment
uvloop # Ultra fast asyncio event loop (Linux/macOS only)
httptools # Fast HTTP parsing
prometheus-client # Metrics export
python-json-logger # Structured JSON logging
tenacity # Retry mechanism for external service calls
sentry-sdk # Error tracking
python-jose[cryptography] # JWT token handling (if using auth)
passlib[bcrypt] # Password hashing (if using auth)
